services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEBUG=true
      - DATABASE_URL=sqlite+aiosqlite:///./itda_dev.db
      - JWT_SECRET_KEY=dev-secret-key
      - TTS_ENGINE=edge
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
